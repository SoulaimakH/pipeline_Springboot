---
- name: Your Playbook Name
  hosts: root@sshcontainer
  gather_facts: no
  tasks:
    - name: Your Task
      debug:
        msg: "Your task here"

vars:
  ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
