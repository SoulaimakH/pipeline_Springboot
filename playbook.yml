---
- name: Install Git on SSH Container
  hosts: sshcontainer
  gather_facts: no  # You can set this to 'yes' if you need to gather facts about the target system

  tasks:
    - name: Create an empty file
      file:
        path: /home/file.txt  # Specify the path 
        state: touch
      become: yes
    
