---
- name: Your Playbook Name
  hosts: root@sshcontainer
  gather_facts: no
  tasks:
    - name: Your Task
      debug:
        msg: "Your task here"
    - name: Create an empty file
    ansible.builtin.shell: ssh -o StrictHostKeyChecking=no root@root@sshcontainer
      file:
        path: /home/file.txt  # Specify the path and filename where you want to create the file
        state: touch
      become: yes
